import React from 'react';

export const ServicioFilters: React.FC = () => {
  return (
    <div className="filters">
      {/* ServicioFilters component implementation */}
    </div>
  );
};

export default ServicioFilters;